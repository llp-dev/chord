*[libm](../../index.md) / [math](../index.md) / [fmod](index.md)*

---

# Module `fmod`

## Quick Reference

| Item | Kind | Description |
|------|------|-------------|
| [`fmodf`](#fmodf) | fn | Calculate the remainder of `x / y`, the precise result of `x - trunc(x / y) * y`. |
| [`fmod`](#fmod) | fn | Calculate the remainder of `x / y`, the precise result of `x - trunc(x / y) * y`. |

## Functions

### `fmodf`

```rust
fn fmodf(x: f32, y: f32) -> f32
```

Calculate the remainder of `x / y`, the precise result of `x - trunc(x / y) * y`.

### `fmod`

```rust
fn fmod(x: f64, y: f64) -> f64
```

Calculate the remainder of `x / y`, the precise result of `x - trunc(x / y) * y`.

