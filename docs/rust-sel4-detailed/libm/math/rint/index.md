*[libm](../../index.md) / [math](../index.md) / [rint](index.md)*

---

# Module `rint`

## Quick Reference

| Item | Kind | Description |
|------|------|-------------|
| [`rintf`](#rintf) | fn | Round `x` to the nearest integer, breaking ties toward even. |
| [`rint`](#rint) | fn | Round `x` to the nearest integer, breaking ties toward even. |

## Functions

### `rintf`

```rust
fn rintf(x: f32) -> f32
```

Round `x` to the nearest integer, breaking ties toward even.

### `rint`

```rust
fn rint(x: f64) -> f64
```

Round `x` to the nearest integer, breaking ties toward even.

