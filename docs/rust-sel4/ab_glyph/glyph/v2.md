**ab_glyph > glyph > v2**

# Module: glyph::v2

## Contents

**Structs**

- [`GlyphImage`](#glyphimage) - A pre-rendered image of a glyph, usually used for emojis or other glyphs

---

## ab_glyph::glyph::v2::GlyphImage

*Struct*

A pre-rendered image of a glyph, usually used for emojis or other glyphs
that can't be represented only using an outline.

**Generic Parameters:**
- 'a

**Fields:**
- `origin: crate::Point` - Offset of the image from the normal origin (top at the baseline plus
- `width: u16` - Image width.
- `height: u16` - Image height.
- `pixels_per_em: u16` - Pixels per em of the selected strike.
- `data: &'a [u8]` - Raw image data, see [`format`](GlyphImageFormat).
- `format: crate::GlyphImageFormat` - Format of the raw [`data`](Self::data).

**Trait Implementations:**

- **Clone**
  - `fn clone(self: &Self) -> GlyphImage<'a>`
- **Debug**
  - `fn fmt(self: &Self, f: & mut $crate::fmt::Formatter) -> $crate::fmt::Result`



