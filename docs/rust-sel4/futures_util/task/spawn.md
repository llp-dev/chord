**futures_util > task > spawn**

# Module: task::spawn

## Contents

**Traits**

- [`LocalSpawnExt`](#localspawnext) - Extension trait for `LocalSpawn`.
- [`SpawnExt`](#spawnext) - Extension trait for `Spawn`.

---

## futures_util::task::spawn::LocalSpawnExt

*Trait*

Extension trait for `LocalSpawn`.

**Methods:**

- `spawn_local`: Spawns a task that polls the given future with output `()` to



## futures_util::task::spawn::SpawnExt

*Trait*

Extension trait for `Spawn`.

**Methods:**

- `spawn`: Spawns a task that polls the given future with output `()` to



