**libm > math > fmod**

# Module: math::fmod

## Contents

**Functions**

- [`fmod`](#fmod) - Calculate the remainder of `x / y`, the precise result of `x - trunc(x / y) * y`.
- [`fmodf`](#fmodf) - Calculate the remainder of `x / y`, the precise result of `x - trunc(x / y) * y`.

---

## libm::math::fmod::fmod

*Function*

Calculate the remainder of `x / y`, the precise result of `x - trunc(x / y) * y`.

```rust
fn fmod(x: f64, y: f64) -> f64
```



## libm::math::fmod::fmodf

*Function*

Calculate the remainder of `x / y`, the precise result of `x - trunc(x / y) * y`.

```rust
fn fmodf(x: f32, y: f32) -> f32
```



