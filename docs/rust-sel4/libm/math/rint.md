**libm > math > rint**

# Module: math::rint

## Contents

**Functions**

- [`rint`](#rint) - Round `x` to the nearest integer, breaking ties toward even.
- [`rintf`](#rintf) - Round `x` to the nearest integer, breaking ties toward even.

---

## libm::math::rint::rint

*Function*

Round `x` to the nearest integer, breaking ties toward even.

```rust
fn rint(x: f64) -> f64
```



## libm::math::rint::rintf

*Function*

Round `x` to the nearest integer, breaking ties toward even.

```rust
fn rintf(x: f32) -> f32
```



