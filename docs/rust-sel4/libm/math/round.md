**libm > math > round**

# Module: math::round

## Contents

**Functions**

- [`round`](#round) - Round `x` to the nearest integer, breaking ties away from zero.
- [`roundf`](#roundf) - Round `x` to the nearest integer, breaking ties away from zero.

---

## libm::math::round::round

*Function*

Round `x` to the nearest integer, breaking ties away from zero.

```rust
fn round(x: f64) -> f64
```



## libm::math::round::roundf

*Function*

Round `x` to the nearest integer, breaking ties away from zero.

```rust
fn roundf(x: f32) -> f32
```



