**libm > math > sincos**

# Module: math::sincos

## Contents

**Functions**

- [`sincos`](#sincos) - Both the sine and cosine of `x` (f64).

---

## libm::math::sincos::sincos

*Function*

Both the sine and cosine of `x` (f64).

`x` is specified in radians and the return value is (sin(x), cos(x)).

```rust
fn sincos(x: f64) -> (f64, f64)
```



