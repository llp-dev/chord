**libm > math > sincosf**

# Module: math::sincosf

## Contents

**Functions**

- [`sincosf`](#sincosf) - Both the sine and cosine of `x` (f32).

---

## libm::math::sincosf::sincosf

*Function*

Both the sine and cosine of `x` (f32).

`x` is specified in radians and the return value is (sin(x), cos(x)).

```rust
fn sincosf(x: f32) -> (f32, f32)
```



