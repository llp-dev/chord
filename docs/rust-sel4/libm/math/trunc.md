**libm > math > trunc**

# Module: math::trunc

## Contents

**Functions**

- [`trunc`](#trunc) - Rounds the number toward 0 to the closest integral value (f64).
- [`truncf`](#truncf) - Rounds the number toward 0 to the closest integral value (f32).

---

## libm::math::trunc::trunc

*Function*

Rounds the number toward 0 to the closest integral value (f64).

This effectively removes the decimal part of the number, leaving the integral part.

```rust
fn trunc(x: f64) -> f64
```



## libm::math::trunc::truncf

*Function*

Rounds the number toward 0 to the closest integral value (f32).

This effectively removes the decimal part of the number, leaving the integral part.

```rust
fn truncf(x: f32) -> f32
```



