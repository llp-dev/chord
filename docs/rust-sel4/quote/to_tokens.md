**quote > to_tokens**

# Module: to_tokens

## Contents

**Traits**

- [`ToTokens`](#totokens) - Types that can be interpolated inside a `quote!` invocation.

---

## quote::to_tokens::ToTokens

*Trait*

Types that can be interpolated inside a `quote!` invocation.

**Methods:**

- `to_tokens`: Write `self` to the given `TokenStream`.
- `to_token_stream`: Convert `self` directly into a `TokenStream` object.
- `into_token_stream`: Convert `self` directly into a `TokenStream` object.



