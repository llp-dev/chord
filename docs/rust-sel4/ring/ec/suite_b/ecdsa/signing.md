**ring > ec > suite_b > ecdsa > signing**

# Module: ec::suite_b::ecdsa::signing

## Contents

**Structs**

- [`EcdsaKeyPair`](#ecdsakeypair) - An ECDSA key pair, used for signing.
- [`EcdsaSigningAlgorithm`](#ecdsasigningalgorithm) - An ECDSA signing algorithm.
- [`PublicKey`](#publickey)

**Statics**

- [`ECDSA_P256_SHA256_ASN1_SIGNING`](#ecdsa_p256_sha256_asn1_signing) - Signing of ASN.1 DER-encoded ECDSA signatures using the P-256 curve and
- [`ECDSA_P256_SHA256_FIXED_SIGNING`](#ecdsa_p256_sha256_fixed_signing) - Signing of fixed-length (PKCS#11 style) ECDSA signatures using the
- [`ECDSA_P384_SHA384_ASN1_SIGNING`](#ecdsa_p384_sha384_asn1_signing) - Signing of ASN.1 DER-encoded ECDSA signatures using the P-384 curve and
- [`ECDSA_P384_SHA384_FIXED_SIGNING`](#ecdsa_p384_sha384_fixed_signing) - Signing of fixed-length (PKCS#11 style) ECDSA signatures using the

---

## ring::ec::suite_b::ecdsa::signing::ECDSA_P256_SHA256_ASN1_SIGNING

*Static*

Signing of ASN.1 DER-encoded ECDSA signatures using the P-256 curve and
SHA-256.

See "`ECDSA_*_ASN1` Details" in `ring::signature`'s module-level
documentation for more details.

```rust
static ECDSA_P256_SHA256_ASN1_SIGNING: EcdsaSigningAlgorithm
```



## ring::ec::suite_b::ecdsa::signing::ECDSA_P256_SHA256_FIXED_SIGNING

*Static*

Signing of fixed-length (PKCS#11 style) ECDSA signatures using the
P-256 curve and SHA-256.

See "`ECDSA_*_FIXED` Details" in `ring::signature`'s module-level
documentation for more details.

```rust
static ECDSA_P256_SHA256_FIXED_SIGNING: EcdsaSigningAlgorithm
```



## ring::ec::suite_b::ecdsa::signing::ECDSA_P384_SHA384_ASN1_SIGNING

*Static*

Signing of ASN.1 DER-encoded ECDSA signatures using the P-384 curve and
SHA-384.

See "`ECDSA_*_ASN1` Details" in `ring::signature`'s module-level
documentation for more details.

```rust
static ECDSA_P384_SHA384_ASN1_SIGNING: EcdsaSigningAlgorithm
```



## ring::ec::suite_b::ecdsa::signing::ECDSA_P384_SHA384_FIXED_SIGNING

*Static*

Signing of fixed-length (PKCS#11 style) ECDSA signatures using the
P-384 curve and SHA-384.

See "`ECDSA_*_FIXED` Details" in `ring::signature`'s module-level
documentation for more details.

```rust
static ECDSA_P384_SHA384_FIXED_SIGNING: EcdsaSigningAlgorithm
```



## ring::ec::suite_b::ecdsa::signing::EcdsaKeyPair

*Struct*

An ECDSA key pair, used for signing.

**Methods:**

- `fn generate_pkcs8(alg: &'static EcdsaSigningAlgorithm, rng: &dyn rand::SecureRandom) -> Result<pkcs8::Document, error::Unspecified>` - Generates a new key pair and returns the key pair serialized as a
- `fn from_pkcs8(alg: &'static EcdsaSigningAlgorithm, pkcs8: &[u8], rng: &dyn rand::SecureRandom) -> Result<Self, error::KeyRejected>` - Constructs an ECDSA key pair by parsing an unencrypted PKCS#8 v1
- `fn from_private_key_and_public_key(alg: &'static EcdsaSigningAlgorithm, private_key: &[u8], public_key: &[u8], rng: &dyn rand::SecureRandom) -> Result<Self, error::KeyRejected>` - Constructs an ECDSA key pair from the private key and public key bytes
- `fn sign(self: &Self, rng: &dyn rand::SecureRandom, message: &[u8]) -> Result<signature::Signature, error::Unspecified>` - Returns the signature of the `message` using a random nonce generated by `rng`.

**Trait Implementations:**

- **KeyPair**
  - `fn public_key(self: &Self) -> &<Self as >::PublicKey`
- **Debug**
  - `fn fmt(self: &Self, f: & mut ::core::fmt::Formatter) -> Result<(), ::core::fmt::Error>`



## ring::ec::suite_b::ecdsa::signing::EcdsaSigningAlgorithm

*Struct*

An ECDSA signing algorithm.

**Traits:** Eq

**Trait Implementations:**

- **Debug**
  - `fn fmt(self: &Self, f: & mut ::core::fmt::Formatter) -> Result<(), ::core::fmt::Error>`
- **PartialEq**
  - `fn eq(self: &Self, other: &Self) -> bool`



## ring::ec::suite_b::ecdsa::signing::PublicKey

*Struct*

**Tuple Struct**: `()`



