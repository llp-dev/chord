**syscalls > syscall > x86_64**

# Module: syscall::x86_64

## Contents

**Functions**

- [`syscall0`](#syscall0) - Issues a raw system call with 0 arguments.
- [`syscall1`](#syscall1) - Issues a raw system call with 1 argument.
- [`syscall2`](#syscall2) - Issues a raw system call with 2 arguments.
- [`syscall3`](#syscall3) - Issues a raw system call with 3 arguments.
- [`syscall4`](#syscall4) - Issues a raw system call with 4 arguments.
- [`syscall5`](#syscall5) - Issues a raw system call with 5 arguments.
- [`syscall6`](#syscall6) - Issues a raw system call with 6 arguments.

---

## syscalls::syscall::x86_64::syscall0

*Function*

Issues a raw system call with 0 arguments.

# Safety

Running a system call is inherently unsafe. It is the caller's
responsibility to ensure safety.

```rust
fn syscall0(n: usize) -> usize
```



## syscalls::syscall::x86_64::syscall1

*Function*

Issues a raw system call with 1 argument.

# Safety

Running a system call is inherently unsafe. It is the caller's
responsibility to ensure safety.

```rust
fn syscall1(n: usize, arg1: usize) -> usize
```



## syscalls::syscall::x86_64::syscall2

*Function*

Issues a raw system call with 2 arguments.

# Safety

Running a system call is inherently unsafe. It is the caller's
responsibility to ensure safety.

```rust
fn syscall2(n: usize, arg1: usize, arg2: usize) -> usize
```



## syscalls::syscall::x86_64::syscall3

*Function*

Issues a raw system call with 3 arguments.

# Safety

Running a system call is inherently unsafe. It is the caller's
responsibility to ensure safety.

```rust
fn syscall3(n: usize, arg1: usize, arg2: usize, arg3: usize) -> usize
```



## syscalls::syscall::x86_64::syscall4

*Function*

Issues a raw system call with 4 arguments.

# Safety

Running a system call is inherently unsafe. It is the caller's
responsibility to ensure safety.

```rust
fn syscall4(n: usize, arg1: usize, arg2: usize, arg3: usize, arg4: usize) -> usize
```



## syscalls::syscall::x86_64::syscall5

*Function*

Issues a raw system call with 5 arguments.

# Safety

Running a system call is inherently unsafe. It is the caller's
responsibility to ensure safety.

```rust
fn syscall5(n: usize, arg1: usize, arg2: usize, arg3: usize, arg4: usize, arg5: usize) -> usize
```



## syscalls::syscall::x86_64::syscall6

*Function*

Issues a raw system call with 6 arguments.

# Safety

Running a system call is inherently unsafe. It is the caller's
responsibility to ensure safety.

```rust
fn syscall6(n: usize, arg1: usize, arg2: usize, arg3: usize, arg4: usize, arg5: usize, arg6: usize) -> usize
```



